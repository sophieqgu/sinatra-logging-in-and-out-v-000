<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if Helper.current_user.is_logged_in? %>
      <%= Helper.current_user.
      <a href="/logout">Log Out</a>
    
  </body>
</html>